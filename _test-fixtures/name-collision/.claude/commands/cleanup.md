---
description: >-
  Project cleanup for this repository. Removes dist/, runs lint:fix, and formats code. Use when the user says "cleanup" or "clean up the codebase".
---

# Project cleanup

> Project-specific cleanup instructions. Not an orchestra skill.

## Process

1. Delete `dist/` folder.
2. Run `npm run lint:fix`.
3. Run `npm run format`.
4. Report remaining lint errors.
