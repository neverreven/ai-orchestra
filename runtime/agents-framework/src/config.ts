// Phase 3b — AgentConfig loader, env merge, preflight validator
// TODO: implement in Phase 3b

export function loadConfig(_projectId: string): never {
  throw new Error("config.ts not yet implemented — Phase 3b");
}
