# ai-orchestra

> Universal, project-agnostic agentic toolkit. Drop it into any project's root, ask any agent in any IDE to "run the orchestra", and the orchestra will investigate the project, respect what's already there, and install a tailored agentic infrastructure — skills, sub-agents, rules, hooks, MCP slots, and a self-improving learnings system — that fits the stack and roles of the project.

## What it is

The ai-orchestra is the practical realisation of an "AI Metaframework" pattern:

- A **slow-evolving core** — this folder. Project-agnostic. Tool-agnostic. Free of any specific project's content.
- A **per-project installed orchestration** — generated by the core for the target project, lives in IDE-native locations (`.cursor/`, `.claude/`, `.vscode/`, `AGENTS.md`, etc.). Adapts itself over time inside that project.
- A **clean session runtime** — the IDE itself. The orchestra never tries to manage what the IDE already does well.

## What it is not

- A monolithic agent.
- A framework you import in code.
- A service that calls home, collects telemetry, or requires an account.
- A replacement for any specific IDE's native features.

It is a **set of markdown specifications** that any sufficiently capable IDE agent can read and act on. v1 ships zero runtime code.

## How to use

Drop the `ai-orchestra/` folder into a project's root, then ask your agent in any supported IDE:

> "Go to the ai-orchestra folder in the root and run the orchestra."

The agent will read [RUN.md](RUN.md) and follow the instructions there, autonomously.

By default, the first run produces a **dry-run diff** — a list of files the orchestra would create, append, or leave alone. Nothing is written until you confirm.

See [RUN.md](RUN.md) for the full bootstrap procedure.

## Status

| Area | State |
|------|-------|
| Core scaffold (this folder) | PR 1 — landing |
| Role library (FE / BE / QA / Analytics / DevOps / Security / Mobile / AI-ML / Tech-Writer / PM) | PR 2 — pending |
| Director + Learnings + Audit + Scheduler/Notifications contracts | PR 3 — pending |
| Cursor adapter (full) | PR 4 — pending |
| Claude Code / Codex / VS Code adapter baselines | PR 5 — pending |
| Stack content packs (JS/TS, Python web, Salesforce / Commerce Cloud) | PR 6 — pending |
| Validation harness + test fixtures | PR 7 — pending |

The current `VERSION` is recorded in the [VERSION](VERSION) file.

## Supported IDEs (v1)

| IDE | Adapter | Coverage |
|-----|---------|----------|
| Cursor | `adapters/cursor/` | Full |
| Claude Code | `adapters/claude-code/` | Baseline |
| Codex CLI | `adapters/codex/` | Baseline |
| VS Code (with Copilot) | `adapters/vscode/` | Baseline |

Adapters for additional tools (Windsurf, Continue, Cody, etc.) are tracked in the v2 backlog.

## Supported stacks (v1, first-class)

- JavaScript / TypeScript web (React, Vue, Svelte, Next, Vite, Node)
- Python web (Django, Flask, FastAPI)
- Salesforce / Commerce Cloud (Apex, LWC, SFRA, sfdx)

Other stacks are still detected by generic signals; deep stack-specific guidance for them lands in v1.1+.

## Repository layout

```
ai-orchestra/
├── README.md          # this file
├── RUN.md             # canonical entry point ("run the orchestra")
├── VERSION            # SemVer
├── CHANGELOG.md       # core evolution log
├── core/              # project-agnostic, tool-agnostic content
│   ├── discovery/     # probe + signals + existing-infra detection
│   ├── roles/         # role definitions (PR 2)
│   ├── skills/        # universal skill specs (PR 2)
│   ├── rules/         # rule templates with placeholders (PR 2)
│   ├── director/      # learnings + audit (PR 3)
│   ├── scheduler/     # declared interface (PR 3)
│   ├── notifications/ # declared interface (PR 3)
│   ├── registry/      # install.json schema
│   ├── prompts/       # prompt templates (PR 2)
│   ├── workflows/     # phase workflows (PR 2)
│   ├── templates/     # output document shells (PR 2)
│   └── stack-packs/   # stack-specific content (PR 6)
├── adapters/          # IDE-specific install logic
│   ├── _contract.md   # adapter interface specification
│   ├── cursor/        # full v1 (PR 4)
│   ├── claude-code/   # baseline v1 (PR 5)
│   ├── codex/         # baseline v1 (PR 5)
│   └── vscode/        # baseline v1 (PR 5)
└── _test-fixtures/    # sample projects for validation (PR 7)
```

## License

See repository root.

## See also

- [RUN.md](RUN.md) — the entry point any agent reads to run the orchestra.
- [core/discovery/DETECTION.md](core/discovery/DETECTION.md) — how the discovery probe works.
- [core/discovery/existing-infra.md](core/discovery/existing-infra.md) — how the orchestra detects and respects prior agentic setup.
- [core/registry/install.schema.md](core/registry/install.schema.md) — shape of the per-project install marker.
- [adapters/_contract.md](adapters/_contract.md) — the binding between core content and IDE-native installation.
