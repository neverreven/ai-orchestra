// Phase 3e — Inter-agent message bus [Opus]
// TODO: implement in Phase 3e

export function openBus(_opts: { root: string; actor: string }): never {
  throw new Error("bus.ts not yet implemented — Phase 3e [Opus]");
}
